command -v colordiff >/dev/null 2>&1 && alias diff="colordiff -u"

command -v htop >/dev/null 2>&1 && alias top=htop

alias ll='ls -lsa --color'

alias path="echo $PATH | tr ':' '\n'"
alias tree='tree -L 2'

command -v composer >/dev/null 2>&1 && alias sf='php app/console'
command -v composer >/dev/null 2>&1 && alias cache='sf cache:clear'
command -v composer >/dev/null 2>&1 && alias routes='sf router:debug'
command -v composer >/dev/null 2>&1 && alias services='sf container:debug'

alias clog='echo "" > ~/temp/dev.log'

alias spec='vendor/bin/phpspec'
alias unit='vendor/bin/phpunit'

alias phps='php -S 0.0.0.0:8888'
# then just phps public/index.php
