#!/usr/bin/env bash

for file in ~/.{aliases,exports,functions}; do
    [[ -r ${file} ]] && . "${file}"
done

[[ -d ${HOME}/workspace/baruica/dotfiles ]] && . "${HOME}/workspace/baruica/dotfiles/.bash_profile"
